# 怎麼加內容

## 純文字：丟一個 txt 進去就好

```
posts/
  002.txt           ← 一個檔案就是一篇純文字貼文
  003.txt
```

## 要配圖：建一個資料夾

```
posts/
  004/
    文案.txt        ← 這篇要發的文字
    1.jpg           ← 要配的圖，直接放進來
    2.jpg
  005/
    1.png           ← 只有圖、沒有文字也可以
```

檔名和資料夾名一起排序，`002.txt` 會排在 `004/` 前面。

## 規則

| 項目 | 規定 |
|---|---|
| 名字 | 決定發布順序，建議 001、002、003…… |
| 話題標籤 | 直接寫在文案裡，例如 `#澳洲生活`。**一篇只認一個**，寫多個只有第一個生效 |
| 文字檔 | 任何 `.txt` 檔。超過 500 字會自動切成一串，每篇約 180 字，圖片放在第一篇 |
| 圖片格式 | 只收 `.jpg`、`.jpeg`、`.png`（iPhone 的 HEIC 會自動轉檔） |
| 影片格式 | `.mp4`、`.mov`，最長 5 分鐘。**GIF 不行**，要先轉成 mp4 |
| 圖片數量 | 1 張＝單圖貼文，2～20 張＝輪播，0 張＝純文字 |
| 圖片順序 | 按檔名排序，所以取名 1.jpg、2.jpg…… |
| 圖片大小 | 每張不超過 8MB |

## 發布時間

每天布里斯本時間 **8、10、12、13、15、16、18、19、20、21 點**，共 10 次，每次發一篇，按名字順序。

- 發完的資料夾會自動移到 `posted/`，並記進 `posted/發布記錄.csv`
- `posts` 空了就不發，不會出錯
- 電腦關機也沒關係，發布是在 GitHub 的伺服器上跑的

## 哪些情況會被跳過

這個資料夾會被留著等你修，然後直接發下一篇：

- 放了不支援的檔案（HEIC、影片、GIF）
- 文字超過 500 字
- 圖片超過 20 張
- 資料夾是空的

在 GitHub 的 Actions 頁面會看到黃色警告，寫明是哪個資料夾、什麼原因。

## 換了 Threads 金鑰之後

金鑰 60 天到期，每月 1 號和 15 號會自動續期，不用管。
真的壞掉的話，去 Meta 後台重新產生一把，存進 GitHub 的 `THREADS_TOKEN`。
