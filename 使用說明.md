# 怎麼加內容

## 一篇貼文 = posts 底下的一個資料夾

```
posts/
  002/
    文案.txt        ← 這篇要發的文字
    1.jpg           ← 要配的圖，直接放進來
    2.jpg
  003/
    文案.txt        ← 只有文字，沒有圖也可以
  004/
    1.png           ← 只有圖，沒有文字也可以
```

## 規則

| 項目 | 規定 |
|---|---|
| 資料夾名字 | 決定發布順序，建議 001、002、003…… |
| 文字檔 | 任何 `.txt` 檔，內容最多 500 字 |
| 圖片格式 | 只收 `.jpg`、`.jpeg`、`.png`（iPhone 的 HEIC 不行） |
| 圖片數量 | 1 張＝單圖貼文，2～20 張＝輪播，0 張＝純文字 |
| 圖片順序 | 按檔名排序，所以取名 1.jpg、2.jpg…… |
| 圖片大小 | 每張不超過 8MB |

## 發布時間

每天布里斯本時間 **9:00、12:00、15:00、18:00、21:00**，每次發一篇，按資料夾名字順序。

- 發完的資料夾會自動移到 `posted/`，並記進 `posted/發布記錄.csv`
- `posts` 空了就不發，不會出錯
- 電腦關機也沒關係，發布是在 GitHub 的伺服器上跑的

## 哪些情況會被跳過

這個資料夾會被留著等你修，然後直接發下一篇：

- 放了不支援的檔案（HEIC、影片、GIF）
- 文字超過 500 字
- 圖片超過 20 張
- 資料夾是空的

在 GitHub 的 Actions 頁面會看到黃色警告，寫明是哪個資料夾、什麼原因。

## 換了 Threads 金鑰之後

金鑰 60 天到期，每月 1 號和 15 號會自動續期，不用管。
真的壞掉的話，去 Meta 後台重新產生一把，存進 GitHub 的 `THREADS_TOKEN`。
